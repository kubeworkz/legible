# Legible Agent Blueprint — Snowflake
#
# A NemoClaw-compatible blueprint optimized for Snowflake.
# Includes SnowSQL CLI and tailored network policies.
#
# Usage:
#   legible agent create my-sf-agent --blueprint legible-snowflake

version: "0.3.0"
min_openshell_version: "0.0.32"
description: |
  Snowflake-optimized Legible agent blueprint. Pre-installed with SnowSQL
  CLI. Network policies allow Snowflake account endpoints and Legible
  MCP/UI access.

supported_connectors:
  - SNOWFLAKE

components:
  sandbox:
    image: "legible-sandbox:latest"
    build:
      dockerfile: "../../Dockerfile"
    name: "legible-sf-agent"
    forward_ports:
      - 9000
    resources:
      cpus: "4.0"
      memory: "16g"
      max_sandboxes: 20

  inference:
    profiles:
      nvidia:
        provider_type: "nvidia"
        provider_name: "nvidia-inference"
        endpoint: "https://integrate.api.nvidia.com/v1"
        model: "nvidia/nemotron-3-super-120b-a12b"
      openai:
        provider_type: "openai"
        provider_name: "openai-inference"
        endpoint: "https://api.openai.com/v1"
        model: "gpt-4o"
      anthropic:
        provider_type: "anthropic"
        provider_name: "anthropic-inference"
        endpoint: "https://api.anthropic.com/v1"
        model: "claude-sonnet-4-20250514"
      local:
        provider_type: "ollama"
        provider_name: "local-inference"
        endpoint: "http://host.docker.internal:11434/v1"
        model: "llama3.1:8b"

  mcp:
    servers:
      legible:
        transport: "streamable-http"
        url: "http://host.docker.internal:9000/mcp"

  tools:
    install:
      - snowsql
    scripts:
      - name: "sf-check"
        command: "snowsql --version"
        description: "Check SnowSQL CLI availability"

policies:
  network: "policies/legible-snowflake.yaml"
  filesystem:
    read_only:
      - /usr
      - /lib
      - /proc
      - /app
      - /etc
    read_write:
      - /home/sandbox
      - /workspace
      - /tmp
  process:
    deny_privilege_escalation: true
    run_as_user: sandbox
    run_as_group: sandbox

agent:
  type: "claude"
  allowed_types:
    - claude
    - codex
    - opencode
    - copilot
  entrypoint: "/usr/local/bin/sandbox-entrypoint.sh"
