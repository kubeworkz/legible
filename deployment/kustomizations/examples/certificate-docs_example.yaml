apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: docs.kubeworkz.io
spec:
  dnsNames:
    - docs.kubeworkz.io
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: kubeworkz.io-prod
  secretName: docs.kubeworkz.io-tls
